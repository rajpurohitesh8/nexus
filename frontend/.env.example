# Frontend Environment Variables
# Copy this file to .env.local for local development

# API base URL (leave empty to use Vite proxy in dev, set for production)
VITE_API_URL=

# Base path (leave as / unless deploying to a sub-path)
BASE_PATH=/
